interface:
  display_name: "n8n API Workflow Ops"
  short_description: "Inspect and build idiomatic n8n workflows"
  default_prompt: "Use $n8n-ops with the plugin-exposed local n8n_rest only. Start with check_connection. No curl, direct HTTP, or legacy config.toml MCP paths."

policy:
  allow_implicit_invocation: true
